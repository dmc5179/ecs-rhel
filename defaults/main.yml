---
# defaults file for ecs-rhel

cluster_name: "redhat"
profile_name: "redhat"
cluster_config: "redhat"
keypair: ""

# Set this to false to use the docker daemon
# Set this to true to use podman
# Note: Podman is not yet supported by this role
podman: true
